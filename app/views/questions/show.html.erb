<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>

<p><%= @question.body %></p>

<% if @question.resolved %>
  <p>Question resolved!</p>
<% else %>
  <p>Question not resolved!</p>
<% end %>
