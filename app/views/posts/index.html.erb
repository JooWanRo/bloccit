<h1>All Posts</h1>
<% @posts.each do |post, ad| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to post.title, post %>
      </h4>
      <p>
        Ad: <%= link_to ad.title, ad %>
      </p>
    </div>
  </div>
<% end %>
